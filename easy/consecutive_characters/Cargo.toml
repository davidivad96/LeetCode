[package]
name = "consecutive_characters"
version = "0.1.0"
edition = "2018"

[[bin]]
name = "consecutive_characters"
path = "consecutive_characters.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
